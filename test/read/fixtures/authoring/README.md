# Fixture authoring recipes

The desktop-PowerPoint COM recipes that produced the `.pptx` fixtures in the parent
directory, plus the Node scripts that derive the committed `*.oracle.json`,
`*.cases.json` and `autofit-calibration.json` sidecars from them.

These are **recipes, not tests.** Nothing here runs in CI, but they do not all need the
same machine: every `author-*.ps1` needs a licensed desktop PowerPoint and an interactive
Windows session, and `measure-lo.py` needs a local LibreOffice, while the `.mjs` builders
are plain cross-platform Node that a Linux contributor can run against the committed
decks. Read
[`.agents/skills/powerpoint-fixture-authoring/SKILL.md`](../../../../.agents/skills/powerpoint-fixture-authoring/SKILL.md)
before running any of them — it carries the COM ordering rules, the teardown/reap
discipline, and the autofit bake-on-save contract that these scripts depend on.

They are tracked so a committed fixture can be re-authored or extended from a clean
checkout. Nothing in this directory is published to npm (`package.json` `files: ["dist"]`).

## Running them

All scripts resolve their own paths from `$PSScriptRoot` / `import.meta.url`, so they can
be invoked from anywhere:

```powershell
& test\read\fixtures\authoring\author-read-stress.ps1        # re-authors ../read-stress.pptx
node test/read/fixtures/authoring/build-oracles.mjs          # re-derives the oracle sidecars
node test/read/fixtures/authoring/gen-cases.mjs --help       # the one here that takes flags
```

`gen-cases.mjs`, `measure-lo.py` and `extract-autofit-calibration.mjs` all lived under
`scripts/` until August 2026 — the first two duplicated there, with nothing keeping the
copies in step. This directory is now the single home for the whole autofit chain, and
for fixture tooling generally: **anything that writes a committed fixture or fixture
sidecar lives here**, whatever it needs to run. `scripts/` keeps build, gate and
maintenance tooling for the repo itself;
[`scripts/README.md`](../../../../scripts/README.md) records why an earlier attempt to
split this directory by platform did not survive contact with its own contents.

Two things to know before you commit the result:

- **Re-run the formatter.** The sidecars are committed in the repo formatter's output,
  but the builders emit raw `JSON.stringify(…, '\t')`. After regenerating, run
  `pnpm exec oxfmt --write "test/read/fixtures/*.json"` or the diff will be pure
  whitespace noise. With that done, every builder here reproduces its committed sidecar
  byte-for-byte — that round-trip is the check that a recipe is still honest.

  That glob deliberately includes `inspect-surface.snapshot.json`, which must **not** be
  reformatted — it is generated one element per line so a changed element is one changed
  line in `git diff`. `.oxfmtrc.jsonc` ignores it by name, so the command above skips it
  on its own; do not work around that by formatting the file directly.
- **A re-authored `.pptx` will not be byte-identical** to the committed one (PowerPoint
  stamps fresh ids/timestamps), so its SHA-256 changes. Update the hash and the
  provenance line in [`../README.md`](../README.md) whenever you replace a fixture, and
  re-verify with the skill's `verify-powerpoint-fixture.ps1`.

## What produces what

| Recipe                                                                                                                                                             | Produces                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `author-deck.ps1`                                                                                                                                                  | **Engine.** Parameterized (`-CasesPath`, `-OutPath`) authoring driver used by `author-all.ps1`; the worked example the authoring skill cites for the pin-then-text-then-AutoSize ordering and the font-readiness guard block.                                                                                                                                                                                  |
| `author-all.ps1` + `gen-cases.mjs` + `measure-lo.py` + `readiness-guard.ps1`                                                                                       | The autofit calibration matrix: `autofit-{shrink,resize,edge,line-metrics}.pptx` and their 4 committed `*.cases.json`. `readiness-guard.ps1` certifies the five required faces actually resolve through GDI (PowerPoint silently substitutes otherwise) and that `soffice` is reachable; `measure-lo.py` reads LibreOffice's recomputed geometry over UNO and feeds `extract-autofit-calibration.mjs`. |
| `author-cjk-wrap.ps1`                                                                                                                                              | `autofit-cjk-wrap.pptx` **and** its `autofit-cjk-wrap.oracle.json` sidecar. The only recipe here that writes its own oracle rather than leaving it to a `.mjs`, and it has to: nothing in a `.pptx` records where a line broke, so the `lines` column exists only because `TextRange.Lines()` is read over COM while the deck is still open. Needs **Malgun Gothic** (Windows-standard, and a plain `.ttf` — the metrics parser cannot open the `.ttc` that MS Gothic, Yu Gothic and SimSun ship as); it guards for it through GDI, like `readiness-guard.ps1` does for the calibration faces. |
| `extract-autofit-calibration.mjs`                                                                                                                                  | `../autofit-calibration.json` — the derived table the Linux test suite reads: PowerPoint's baked outputs per case-id, merged with the LibreOffice column when `measure-lo.py`'s `<deck>.lo.json` files are present (`--lo-dir`, default `<repo>/.tmp`). The only script here that needs neither PowerPoint nor LibreOffice. It carries over a previously-committed LibreOffice column when those uncommitted scratch files are absent, so re-running it on Linux narrows nothing.                                            |
| `build-font-metrics.mjs`                                                                                                                                            | `../autofit-font-metrics.json` — the advance widths the autofit and CJK oracles measure with where the fonts are absent. Needs **all six faces installed** (Aptos and Aptos SemiBold come from Microsoft 365, the rest ship with Windows) and refuses to write a partial sidecar: a face missing here is a case that skips there, silently. Needs no PowerPoint. |
| `author-read-stress.ps1`                                                                                                                                           | `read-stress.pptx` — the multi-dimension integration fixture (two masters/themes, nested groups, styled tables, dual raster+SVG picture, recolor, embedded fonts, threaded comments, notes).                                                                                                                                                                                                                   |
| `author-placeholder-override.ps1`                                                                                                                                   | `placeholder-override.pptx` — the ground truth for placeholder inheritance vs override: the layout's content placeholder states a frame, a bottom anchor and a 1in left inset, and the slide's overrides the first two. What PowerPoint writes on the slide is only what was overridden, which is the rule `gen/define/text.ts` implements. |
| `author-shadow-shape-vs-text.ps1`                                                                                                                                   | `shadow-shape-vs-text.pptx` — one text box per shadow state (shape only, text only, both), each built from the gesture PowerPoint itself offers. Shows that `p:spPr/a:effectLst` and `a:rPr/a:effectLst` are separate effects and that no single action produces the pair. |
| `author-shape-line-style-override.ps1` | `shape-line-style-override.pptx` — four rectangles with one preset shape style, three of them with a single outline property changed afterwards (weight, dash, colour). PowerPoint writes only the changed property into `spPr/a:ln` and keeps painting the rest from `p:style/a:lnRef`. The recipe reopens the saved deck and exports slide 1 to `.tmp/`, because the pixels are the evidence: `Line.ForeColor` reports the resolved model, not what paints. |
| `author-table-merge-encoding.ps1` | `table-merge-encoding.pptx` — the span attributes PowerPoint writes on a merged region's covered cells: a 2x2 merge, the same merge after a row and a column are inserted through it, a 3x3 merge after a row and a column are deleted through it, and a merge along each single axis. Plain COM: `Cell.Merge`, `Rows.Add`, `Columns.Add` and `Delete` are all scriptable. |
| `author-table-text-inheritance.ps1` | `table-text-inheritance.pptx` — a text box and three tables (the default table style, "No Style, No Grid", and none) on a deck whose `p:defaultTextStyle`, master `p:otherStyle`, theme `dk1` and theme minor font each carry a value no other tier has. None of those edits has a COM surface, so the recipe injects them into the saved package, has PowerPoint reopen and re-save it, then prints each run's resolved font over COM and exports the slide to `.tmp/`. It checks through GDI that Courier New, Georgia and Verdana are installed, since PowerPoint writes a missing face's name and substitutes it only when painting. |
| `author-slide-jump-link.ps1` | `slide-jump-link.pptx` and `slide-jump-link-target-deleted.pptx` — a deck whose second slide jumps to its first from a run and from a shape, with that first slide in a section and a custom show, saved before and after `Slide.Delete()` removes it. What PowerPoint leaves of the links, the section and the custom show is the target for `removeSlide`. The recipe reopens the second deck and prints the actions COM reports, sections and show sizes. |
| `author-chart-legend-entry.ps1` | `chart-legend-entry.pptx` — two combo charts whose legends list their series out of `c:idx` order, each with its first legend entry deleted through `LegendEntries(1).Delete()`. The `c:legendEntry/c:idx` PowerPoint writes is the evidence that it counts legend positions. Plain `pwsh` COM, with the chart workbook handling `author-chart-series-shapes.ps1` uses. The recipe exports each slide to `.tmp/` before and after the deletion, so which label went is visible. |
| `author-table-styles.ps1`                                                                                                                                          | `table-styles.pptx`. Applying a _built-in_ style is what makes PowerPoint materialize real definitions into `ppt/tableStyles.xml`; only Microsoft built-in style GUIDs are used, so the fixture stays brand-free.                                                                                                                                                                                              |
| `author-slide-transition.ps1`, `author-slide-transition-sound.ps1`                                                                                                 | `slide-transition{,-sound}.pptx`. The sound fixture embeds `assets/ding.wav`.                                                                                                                                                                                                                                                                                                                                  |
| `author-slide-animation-{basic,presets,rich}.ps1`                                                                                                                  | The three `slide-animation-*.pptx` timing/build fixtures.                                                                                                                                                                                                                                                                                                                                                      |
| `author-import-animation-merge.ps1`                                                                                                                                | `import-animation-merge.pptx` — the cross-slide paste-with-animation spid-remap ground truth.                                                                                                                                                                                                                                                                                                                  |
| `author-smartart-families.ps1`                                                                                                                                     | `smartart-families.pptx` — the SmartArt node → drawn-shape mapping oracle, one slide per layout family. Arrow labels have no COM surface, so slide 2 takes the inject-then-reopen route `author-table-cell-horzoverflow.ps1` uses; the script's findings block prints which injected labels PowerPoint kept. |
| `probe-smartart-transition-labels.ps1`                                                                                                                              | **Probe; produces no committed fixture.** Which SmartArt layouts let a `parTrans`/`sibTrans` point carry a label — nothing in a package records it and there is no COM surface to ask. Injects a label into every transition point of 16 layouts and has PowerPoint reopen and re-save: it strips transition text a layout has no place for, so what survives is the answer. Cited from [`../README.md`](../README.md); output lands in `.tmp/`. |
| `probe-smartart-mapping.mjs`                                                                                                                                       | **Probe; produces no committed fixture.** Resolves every authored point of every diagram in `smartart-families.pptx` and `mixed.pptx` through its `presOf` connections to a `dsp:sp` and a paragraph index, and exits non-zero if a string fails to resolve — so it doubles as a regression check on a re-authored fixture. Needs neither PowerPoint nor Windows. |
| `author-anim-probe.ps1`, `probe-animeffect.ps1`, `parse-animeffect.ps1`                                                                                            | Probe decks + `animeffect-order.json` behind the `MsoAnimEffect` findings in [`docs/animations-and-transitions.md`](../../../../docs/animations-and-transitions.md).                                                                                                                                                                                                                                           |
| `author-table-cell-horzoverflow.ps1`                                                                                                                               | `table-cell-horzoverflow.pptx`. The attribute has no COM or UI surface, so this one injects it into `slide1.xml` and then has PowerPoint reopen and re-save the deck — the committed bytes are PowerPoint's own serialization. An explicit `horzOverflow="clip"` is dropped by that round-trip (it is the schema default), which is why the fixture pins only `overflow` plus a bare control.                     |
| `probe-combo-legend-order.mjs` → `probe-combo-legend-order.ps1` | **Probe; produces no committed fixture.** Builds combo charts that differ in chart-type order, axis group and series counts, and renders them in PowerPoint, so each legend's order can be read off the picture. Within one axis group the legend follows plotArea order, and across axis groups a bar subchart is listed first. Needs a built `dist/`. Cited from `src/gen/chart/chart-xml.ts`; output lands in `.tmp/`. |
| `probe-default-text-style-paste.ps1` | **Probe; produces no committed fixture.** Pastes `default-text-style.pptx`'s slide into `table-text-inheritance.pptx`, whose `p:defaultTextStyle` differs, once with the destination theme and once keeping source formatting, and reads both pasted runs back. Keeping source formatting, PowerPoint writes each run's resolved size, colour and face onto it rather than letting the destination's default text style reach it, which is what the `preserve` import mirrors. Cited from `src/read/api/ops/flatten.ts`; output lands in `.tmp/`. |
| `probe-scatter-x-cache-format.mjs` → `probe-scatter-x-cache-format.ps1` | **Probe; produces no committed fixture.** Builds a scatter whose X cache and X axis disagree on the number format, then the same bytes with only the cache patched and with only the axis unlinked, and renders all three in PowerPoint. A source-linked axis paints its cache's format, which is why the writer states an X axis format unlinked. Needs a built `dist/`. Cited from `src/gen/chart/chart-axes.ts`; output lands in `.tmp/`. |
| `probe-table-cell-wrap.ps1`                                                                                                                                        | **Probe; produces no committed fixture.** Establishes that a table cell cannot be made non-wrapping (`WordWrap` is read-only over COM, and `<a:bodyPr wrap="none"/>` renders inert and is stripped on the next save) and that `a:tcPr/@horzOverflow` is a glyph clip/overflow control rather than a wrap switch. Cited from `src/gen/slide/objects/table.ts`; all output lands in `.tmp/`.                       |
| `probe-table-cell-a11y-and-3d.ps1`                                                                                                                                 | **Probe; produces no committed fixture.** Settles three `a:tc`/`a:tcPr` constructs with no COM or UI surface, by injecting all three and having PowerPoint reopen and re-save: `a:tc/@id` and `a:tcPr/a:headers` (the screen-reader header association) are **stripped**, while `a:tcPr/a:cell3D` survives verbatim. Controlled rather than circumstantial — `a:cell3D` and `a:headers` went into the *same* `a:tcPr` and only one came back — which is what makes header association a documented non-authorable construct rather than an unimplemented one. Cited from `src/read/api/table.ts` and [`docs/tables.md`](../../../../docs/tables.md); output lands in `.tmp/`. |
| `probe-entryeffect-table.ps1` → `parse-entryeffect-table.mjs` → `entryeffect-table.json`                                                                           | The probed `PpEntryEffect → {element, ns, variant, modernOnly}` table (159 accepted ints, `0` = none). **`build-oracles.mjs` reads `entryeffect-table.json` as a required input**, so it is committed here rather than regenerated per run — reproducing it needs a full 0..4096 COM sweep.                                                                                                                    |
| `build-oracles.mjs`                                                                                                                                                | `slide-transition.oracle.json` (embeds the table above) + the `slide-animation-{basic,rich}` oracles.                                                                                                                                                                                                                                                                                                          |
| `build-{sound,merge,presets}-oracle.mjs`                                                                                                                           | The `slide-transition-sound`, `import-animation-merge` and `slide-animation-presets` oracles.                                                                                                                                                                                                                                                                                                                  |
| `author-omml.ps1` / `author-omml-inline.ps1`                                                                                                                       | `math-omml.pptx` (display equation) and `math-omml-inline.pptx` (inline, no `m:oMathPara`). Both build a genuine equation in Word first rather than hand-typing OMML.                                                                                                                                                                                                                                          |
| `author-modern-comments.ps1`                                                                                                                                       | `modern-comments.pptx`. Modern (2018) comments need `Comments.Add2` with an explicit `providerId`; an empty one raises "Illegal value" and silently falls back to legacy comments.                                                                                                                                                                                                                             |
| `author-picture-media.ps1`                                                                                                                                         | `picture-media.pptx`. See the asset note below.                                                                                                                                                                                                                                                                                                                                                                |
| `author-slide-background.ps1`                                                                                                                                                       | `slide-background.pptx` — the slide-scoped background oracle (picture, theme `p:bgRef`, transparent solid). Slide 2 takes the inject-then-reopen route: `p:bgRef` is what the Background Styles *gallery* writes, and a gallery has no `ExecuteMso` surface while `Slide.Background.Fill` reaches only `p:bgPr`. |
| `make-cube-glb.mjs` → `author-model3d.ps1` | `assets/cube.glb` then `model3d.pptx` — the `am3d:model3d` graphic-frame oracle. Plain `pwsh` COM is reliable for `Shapes.Add3DModel`; no `cscript`/retry needed. The `.glb` is generated rather than downloaded so the fixture input is license-clean by construction — and PowerPoint re-exports it on insert anyway, so the stored part is Microsoft's own GLTF export of it, not the input bytes. |
| `author-chart-series-shapes.ps1` | `chart-series-shapes.pptx` — the chart caches that are not a `c:val` beside a flat `c:cat`: a scatter, a bubble, a two-level category axis, a pie labelled on its series, a scatter and a bubble whose X column holds one text cell, and a scatter and a bubble with no X column. Plain `pwsh` COM. Each chart's embedded workbook is filled cell by cell, text and numbers through separate `Value2` assignments, and a bubble's range starts below the header row, which a bubble chart does not treat as headers. `SetSourceData` over a text column A does not build the text-X bubble, so that chart is sourced from its Y and size columns and its series is given the X range afterwards. Check the saved cache rather than the run: a series assignment the recipe never made fails nothing. After closing each workbook the recipe waits for its Excel to exit, because a next chart that attaches to the exiting instance fails with 0x800706BA. |
| `author-bar-chart.ps1`, `author-default-text-style.ps1`, `author-footer-trio.ps1`, `author-online-video.ps1`, `author-template-potx.ps1`, `extend-multi-theme.ps1` | The correspondingly named fixtures. `author-online-video.ps1` deliberately targets a path under `C:\Users\Public` so the committed fixture's external link carries no user directory.                                                                                                                                                                                                                          |
| `make-assets.ps1`                                                                                                                                                  | Regenerates the programmatic media (`photo.png`, `mark.png`) into `<repo>/.tmp/media/` — gradient/shape rasters drawn in code, so they are license-clean by construction.                                                                                                                                                                                                                                      |

## Assets

`assets/ding.wav` is committed: 844 bytes of self-generated 16-bit PCM mono 8 kHz sine,
which is what `slide-transition-sound.oracle.json` cites for its license-clean claim.

`assets/cube.glb` is committed too, and is likewise generated rather than sourced — 1516
bytes emitted by `make-cube-glb.mjs` (one mesh, 24 vertices, one PBR material, no textures).
Regenerate it with `node test/read/fixtures/authoring/make-cube-glb.mjs`; the output is
deterministic, so a clean checkout reproduces the committed bytes.

The other source images these recipes once consumed are **not** committed. In particular
the SVG behind `picture-media.pptx`'s `SvgPic` shape was a Microsoft Office stock icon
(_Insert → Icons_) — that is fine baked inside a fixture whose provenance says so, but
not as a standalone file in this repo. To re-author that fixture, re-insert an icon the
same way rather than looking for a checked-in copy.

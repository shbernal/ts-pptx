// VitePress only looks for a theme at `<root>/.vitepress/theme`. The theme itself lives in
// `www/`, with the rest of the site's application code — see `www/README.md`.
export { default } from '../../../www/theme'
